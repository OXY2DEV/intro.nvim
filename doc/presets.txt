╭──────────────────────────────────────────────────────── *intro.nvim-presets* ╮
│ 🔌 Presets                                `For those who prefer simplicity ` │
╰────────────────────────────────────────────────────────────────────────────╯

               ██████▒▒░░      ⠛⣿⠛ ⣿⣄  ⣿ ⠛⠛⣿⠛⠛ ⣾⠛⠛⣷ ⣾⠛⠛⣷
               ██░░▒▒██▒▒░░     ⣿  ⣿ ⣿ ⣿   ⣿   ⣿⠶⠶⠟ ⣿  ⣿
               ██░░▒▒████▒▒░░   ⣿  ⣿ ⣿ ⣿   ⣿   ⣿⠷   ⣿  ⣿
               ██▒▒▒▒░░▒▒▒▒░░  ⣤⣿⣤ ⣿  ⠙⣿   ⣿   ⣿ ⢷  ⢿⣤⣤⡿.nvim
               ██████▒▒▒▒░░    
               ▒▒▒▒▒▒▒▒░░      `Yet another start screen plugin`

If you want to quickly have a `start screen` ready or don't like complexity,
then you are most likely love `presets`. Presets provide ways to customise
the `start screen` without writing long configs.

`╭──────────────────────────────────────────────────────────┤` Table of contents
`│`
`├──` Preset list                                         |intro.nvim-preset-list|
`├──` Preset usage
`├──` Preset: nvim                                             |intro.nvim-p-nvim|
`├──` Preset: nvim_alt_1                                 |intro.nvim-p-nvim_alt_1|
`├──` Preset: startify                                     |intro.nvim-p-startify|
`╰──` Preset: cats                                             |intro.nvim-p-cats|

`╭────────────────────────────────────────────────────` *intro.nvim-preset-list* `╮`
`│` 🌄 Preset list                                                          🧭 `│`
`╰────────────────────────────────────────────────────────────────────────────╯`
Here is a list of all the available presets,
		 1. nvim
		 2. nvim_alt_1
		 3. startify
		 4. cats

`╭────────────────────────────────────────────────────────────────────────────╮`
`│` 🌸 Preset usage                                                         🧭 `│`
`╰────────────────────────────────────────────────────────────────────────────╯`
Presets are used by setting the {preset} property in `setup()` function.

You can set a `preset` with default values by putting the name of the `preset`
in the {name} porperty in {preset}

All presets provide some `options` which you can used by adding that options
name to the {opts} table in {preset}.

Usage ~
`╭` Lua `───────────────────────────────────────────────────────────────────────╮`
>
  require("intro").setup({
    preset = {
      name = "nvim",
			opts = { "animated" }
		}
  })
<
`╰────────────────────────────────────────────────────────────────────────────╯`

The order of items in {opts} is IMPORTANT. Options that change colors or
animations need to be applied `before` and options that add components or don't
change anything directly should be applied `after`.

`╭─────────────────────────────────────────────────────────` *intro.nvim-p-nvim* `╮`
`│` 💥 Preset: nvim                                                          N `│`
`╰────────────────────────────────────────────────────────────────────────────╯`
Shows a simple `Neovim` logo text art.

This `preset` has the following options,
		 1. animated              `Adds a simple reveal animation`

`╭───────────────────────────────────────────────────` *intro.nvim-p-nvim_alt_1* `╮`
`│` 💥 Preset: nvim_alt_1                                                    N `│`
`╰────────────────────────────────────────────────────────────────────────────╯`
Shows a `Neovim` text art and optionally recent files.

This `preset` has the following options,
		 1. dark                  `Gradient color between #89b5fa & #a6e3a1"`
		 2. dark_alt              `Gradient color between #6dd5fa & #2980b9`
		 3. dark_animated         `Pulse animation for "dark"`
		 4. dark_alt_animated     `Pulse animation for "dark_animated"`
		 5. recents               `Adds a recent files component`


`╭` When & where to use each options `──────────────────────────────────────────╮`
`│` If you just want the colors use `dark` or `dark_alt`. If you want animations   `│`
`│` just use the `_animated` varients. No need to use `dark` & `dark_animated` at    `│`
`│` the same time.                                                             `│`
`│`                                                                            `│`
`│` If you want `recent files` too you can use `recents`. Just add it after the    `│`
`│` colors.                                                                    `│`
`╰────────────────────────────────────────────────────────────────────────────╯`

`╭─────────────────────────────────────────────────────` *intro.nvim-p-startify* `╮`
`│` 💥 Preset: startify                                  `Based on vim-startify │`
`╰────────────────────────────────────────────────────────────────────────────╯`
Shows a `Neovim` text art and optionally recent files(global & local).

This `preset` has the following options,
		 1. blue                     `Changes the art color to #89b4fa`
		 2. green                    `Changes the art color to #a6e3a1`
		 3. red                      `Changes the art color to #f38ba8`
		 4. pink                     `Changes the art color to #f5c2e7`
		 5. flamingo                 `Changes the art color to #f2cdcd`
		 6. gradient_blue_green      `Changes the art color to grdient between`
		                             `#89b4fa and #a6e3a1`
		 7. gradient_endless_river   `Changes the art color to grdient between`
                                 `#43cea2 and #185a9d`
		 8. gradient_friday          `Changes the art color to grdient between`
                                 `#83a4d4 and #b6fbff`
		 9. recents                  `Adds a recent files component`
		 10. recents_in_current_dir  `Adds a recent files component foe the`
		                             `current directory`
		 11. list_shade              `Adds a shade to all the recent file names`

`╭` When & where to use each options `──────────────────────────────────────────╮`
`│` Color options(1 - 8) should be added first, followed by the components(how `│`
`│` the components are shown are also determined by which option is added      `│`
`│` first).                                                                    `│`
`│`                                                                            `│`
`│` `list_shade` can be after other options or after color options.              `│`
`╰────────────────────────────────────────────────────────────────────────────╯`

`╭─────────────────────────────────────────────────────────` *intro.nvim-p-cats* `╮`
`│` 💥 Preset: cats                         `Based on the catppuccin wallpapers │`
`╰────────────────────────────────────────────────────────────────────────────╯`
Shows a bunch of `cats` on the start screen.

This `preset` has the following options,
		 1. 1x2                  `Shows 2 cats side-by-side`
		 2. 1x3                  `Shows 3 cats side-by-side`
		 3. 1x4                  `Shows 4 cats side-by-side`
		 4. 2x2                  `Shows 4 cats`
		 5. 3x3                  `Shows 9 cats`
		 6. 3x1                  `Shows 3 cats on top of each other`
		 7. rosewater            `Cats color becomes #F5E0DC`
		 8. rosewater_alt        `Cats color becomes #DC8A78`
		 9. mauve                `Cats color becomes #CBA6F7`
		 10. mauve_alt           `Cats color becomes #8839EF`
		 11. yellow              `Cats color becomes #F9E2AF`
		 12. yellow_alt          `Cats color becomes #DF8E1D`
		 13. green               `Cats color becomes #A6E3A1`
		 14. green_alt           `Cats color becomes #40A02B`
		 15. blue                `Cats color becomes #89B4FA`
		 16. blue_alt            `Cats color becomes #1E66F5`
		 17. all_the_colors      `Adds all of the colors to different cats`
		 18. all_the_colors_alt  `Adds all the _alt colors to different cats`

`╭` When & where to use each options `──────────────────────────────────────────╮`
`│` Cat options(1 - 6) should be applied first followed by one of the color    `│`
`│` options(7 - 18).                                                           `│`
`╰────────────────────────────────────────────────────────────────────────────╯`


`─────────────────────────────────────────────────────────────────────────────┤`
vim:tw=78:ts=2:noet:ft=help:norl:
